{
  "name": "proof-of-donation",
  "version": "0.1.0",
  "description": "Self-hostable donation-receipt verification server. Verifies DKIM-signed charity donation receipt emails and reports the donation amount, currency, date, and donor email, so a caller can require proof of a recent donation without a human reviewer.",
  "type": "module",
  "license": "MIT",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts",
    "test": "node --import tsx --test src/**/*.test.ts"
  },
  "dependencies": {
    "express": "^4.22.2",
    "mailauth": "^4.13.3",
    "postal-mime": "^3.0.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.25",
    "@types/node": "^22.20.2",
    "tsx": "^4.23.13",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=20.18.1"
  }
}
